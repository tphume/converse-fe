import React from "react";
import Typography from "@material-ui/core/Typography";

export default function Help(): JSX.Element {
  return (
    <section>
      <Typography variant="h2">You are at Help page</Typography>
    </section>
  );
}
