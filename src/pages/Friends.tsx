import React from "react";
import Typography from "@material-ui/core/Typography";

export default function Friends(): JSX.Element {
  return (
    <section>
      <Typography variant="h1">You are at Friends page</Typography>
    </section>
  );
}
